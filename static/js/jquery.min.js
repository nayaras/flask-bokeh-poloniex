$('#time').change(function () {
    //location.reload();
    var selected_index = $('#time :selected').val();
    console.log('val', selected_index)
    localStorage.setItem("time", selected_index);
    $.ajax({
        type: "GET",
        url: '/',
        data: {
            'selected_index': selected_index
        },
        success: function (response) {
            console.log("foi");
            downString = response.downloadString;
            //$("#candlestick").load("/");
            window.location = window.location.href;
        }
    });
    //window.location.reload();
});
$(document).ready(function () {
    if ($('#time').val() == null) {
        localStorage.setItem("time", "1");
    }
    if ($('#time').length) {
        $('#time').val(localStorage.getItem("time"));

    }


    //console.log('tam ',$('#time').length, 'tam 2', $('#time :selected').val())
});
// window.onbeforeunload = (function() {
//     //localStorage.removeItem("time");
//     localStorage.clear();
// });